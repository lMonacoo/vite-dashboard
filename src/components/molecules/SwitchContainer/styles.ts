import styled from 'styled-components';

export const SwitchContainer = styled.div`
  position: relative;
  margin-top: 0.1rem;
  height: fit-content;

  display: flex;
  align-items: center;
  gap: 1rem;
`;
